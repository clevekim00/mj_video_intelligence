[project]
name = "mj-video-intelligence-demo-analysis"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn>=0.30,<1",
  "yt-dlp>=2026.8.12,<2027",
  "python-multipart>=0.0.20,<1",
  "faster-whisper>=1.2,<2",
]

[project.optional-dependencies]
test = ["httpx>=0.27,<1"]

[tool.pytest.ini_options]
pythonpath = [".", "../.."]
